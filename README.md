# YOUR_PROJECT_NAME

> YOUR_PROJECT_DESCRIPTION

A Next.js e-commerce storefront with an admin dashboard, Supabase backend, payment integrations, and an AI shopping assistant.

## Getting Started

```bash
npm install
npm run dev
```

The app runs on [http://localhost:3009](http://localhost:3009).

## Environment Variables

Copy `.env.example` to `.env.local` and fill in your own values:

```bash
cp .env.example .env.local
```

See `.env.example` for the full list of required and optional variables.

## Database

SQL migrations live in `supabase/`. See `supabase/README.md` for setup instructions.

## Create an Admin User

```bash
npm run create-admin
```

## Deployment

YOUR_DEPLOYMENT_INSTRUCTIONS

## Customization

This codebase ships brand-neutral. See [`CUSTOMIZE.md`](./CUSTOMIZE.md) for the full checklist of placeholders to replace and assets to add.

## License

YOUR_LICENSE
